import java.io.*;

public class Main {
    public static void main(String[] arg) throws FileNotFoundException {



    }

    void Entrance () throws IOException {
        File file = new File("vorodi.txt") ;
        BufferedReader in;
        in = new BufferedReader(new FileReader(file));

        String st;
        while (!(st = in.readLine()).equals("END")){

            if (st.charAt(0)!='*'){
                if ()
            }

        }
    }
}
